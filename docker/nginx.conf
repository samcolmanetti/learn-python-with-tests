server {
    listen 3000;
    listen [::]:3000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve clean URLs (/numbers) as well as /numbers.html.
    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
