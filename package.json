{
  "name": "learn-python-with-tests",
  "version": "0.1.0",
  "private": true,
  "description": "GitBook build tooling for Learn Python with Tests.",
  "scripts": {
    "serve": "honkit serve",
    "build": "honkit build . _book",
    "start": "serve _book --listen 3000 --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "gitbook-plugin-code": "^0.1.0",
    "honkit": "^6.0.0",
    "honkit-plugin-pagetoc": "file:tools/honkit-plugin-pagetoc"
  }
}
